# Dependencies
node_modules/
vendor/
composer.lock

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Build output
dist/
build/
*.log

# Docker
.docker/
docker-compose.override.yml

# OS
Thumbs.db
.DS_Store

# Temporary files
*.tmp
.cache/
.temp/

# Database
*.sql
# 迁移脚本必须纳入版本库，否则换环境无法重建表结构
!db/migrations/*.sql
# 建库入口同理 —— docker-entrypoint 执行的就是它，被 *.sql 挡在版本库外的话，
# 克隆仓库的人拿不到、新机器根本建不起库。它长期缺失也是这张表结构里
# asset_types 漏建、shareholders 顺序颠倒这些问题一直没被发现的原因。
!api/config/init.sql
*.sqlite
*.db

# Node
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# Vite
.vite/

# PHP
.phpstan/
.phpunit.result.cache

# Testing
coverage/
.nyc_output/
node_modules/
dist/

# 测试产物：浏览器截图，每次跑测试都会重新生成
test-screenshots/
.claude/settings.local.json
