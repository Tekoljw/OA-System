# OA 财务管理系统 — 完整测试计划

**版本**: v1.0  
**日期**: 2026-08-28  
**测试地址**: https://oa.starway.sg  
**默认账号**: admin / admin123  

---

## 一、测试模块总览

| 编号 | 模块 | 测试项数 | 优先级 |
|------|------|---------|--------|
| M1 | 认证与权限 | 12 | P0 |
| M2 | 账户管理 | 14 | P0 |
| M3 | 出入金交易 | 16 | P0 |
| M4 | 内部划款 | 12 | P0 |
| M5 | 仪表板与报表 | 10 | P1 |
| M6 | 配置管理 | 16 | P1 |
| M7 | 人员管理 | 8 | P1 |
| M8 | 活动日志 | 6 | P2 |
| M9 | 数据安全 | 8 | P0 |
| M10 | UI 交互与响应式 | 8 | P2 |
| **合计** | | **110** | |

---

## 二、分模块测试用例

---

### M1 认证与权限（12 项）

#### M1.1 登录流程

| 编号 | 用例 | 操作步骤 | 预期结果 |
|------|------|---------|---------|
| M1-01 | 空表单提交 | 不输入用户名密码，点击登录 | 表单校验拦截，不发送请求，显示必填提示 |
| M1-02 | 仅输入用户名 | 输入 admin，密码留空，点击登录 | 表单校验拦截，提示密码必填 |
| M1-03 | 错误密码 | 输入 admin / wrong123，点击登录 | 停留在登录页，显示"用户名或密码错误"提示 |
| M1-04 | 不存在的用户 | 输入 nouser / 123456，点击登录 | 停留在登录页，显示错误提示 |
| M1-05 | 正确登录 | 输入 admin / admin123，点击登录 | 跳转到仪表板首页，侧边栏显示用户名 |

#### M1.2 Token 与会话

| 编号 | 用例 | 操作步骤 | 预期结果 |
|------|------|---------|---------|
| M1-06 | Token 存储 | 登录后检查 localStorage | 存在 `token` 字段，长度 > 100 |
| M1-07 | 项目信息存储 | 登录后检查 localStorage | 存在 `currentProject` 字段（JSON 格式，含 id） |
| M1-08 | 无效 Token 访问 | 用伪造 Token 调用 GET /api/accounts | 返回 401 Unauthorized |
| M1-09 | 无 Token 访问 | 不带 Authorization 头调用 GET /api/accounts | 返回 401 Unauthorized |
| M1-10 | Token 过期处理 | 使用过期 Token 调用 API | 返回 401，前端自动跳转到登录页 |

#### M1.3 登出

| 编号 | 用例 | 操作步骤 | 预期结果 |
|------|------|---------|---------|
| M1-11 | 正常登出 | 点击退出登录按钮 | 清除 localStorage，跳转到登录页 |
| M1-12 | 登出后访问保护页面 | 登出后直接访问 / 路径 | 重定向到 /login |

---

### M2 账户管理（14 项）

路径: `/assets/records`

#### M2.1 账户列表

| 编号 | 用例 | 操作步骤 | 预期结果 |
|------|------|---------|---------|
| M2-01 | 页面加载 | 导航到账户管理页面 | 显示已有账户列表（卡片或表格），不显示空白 |
| M2-02 | 账户信息展示 | 查看账户卡片 | 每个账户显示：名称、账号、币种、余额、状态 |
| M2-03 | 按币种筛选 | 点击币种 Tab（如 CNY/USD） | 仅显示对应币种的账户 |
| M2-04 | 按类型筛选 | 点击账户类型 Tab | 仅显示对应类型的账户 |

#### M2.2 创建账户

| 编号 | 用例 | 操作步骤 | 预期结果 |
|------|------|---------|---------|
| M2-05 | 打开创建表单 | 点击"添加账户"按钮 | 弹出对话框，包含币种、类型、名称、账号、银行、额度字段 |
| M2-06 | 必填验证 | 不填任何字段，点击提交 | 表单校验拦截，显示各字段必填提示 |
| M2-07 | 正常创建 | 填写完整信息（币种=CNY，类型=活期，名称=测试户，账号=T001，银行=测试银行，额度=100000），提交 | 创建成功，对话框关闭，账户列表中出现新账户 |
| M2-08 | API 创建 — 空名称 | POST /api/accounts，name 为空 | 返回 400，message 含"名称不能为空" |
| M2-09 | 币种/类型下拉数据 | 打开创建表单，查看下拉选项 | 币种显示 CNY/USD/EUR/JPY，类型显示系统配置的账户类型 |

#### M2.3 编辑与删除

| 编号 | 用例 | 操作步骤 | 预期结果 |
|------|------|---------|---------|
| M2-10 | 编辑账户 | 点击账户的编辑按钮，修改名称，保存 | 更新成功，列表中显示新名称 |
| M2-11 | 编辑回显 | 打开编辑对话框 | 表单中预填当前账户信息（名称、币种、类型等） |
| M2-12 | 删除账户 | 点击账户的删除按钮，确认删除 | 账户从列表中消失 |
| M2-13 | 删除确认弹窗 | 点击删除按钮 | 弹出确认对话框，非直接删除 |
| M2-14 | 删除不存在的账户 | API: DELETE /api/accounts/99999 | 返回 400 或 404，提示"账户不存在" |

---

### M3 出入金交易（16 项）

路径: `/external-transactions`

#### M3.1 交易列表

| 编号 | 用例 | 操作步骤 | 预期结果 |
|------|------|---------|---------|
| M3-01 | 页面加载 | 导航到出入金页面 | 显示交易记录列表，含日期、类型、金额、账户、科目、状态 |
| M3-02 | 按币种 Tab 切换 | 点击 CNY/USD 等 Tab | 统计卡片和列表切换到对应币种数据 |
| M3-03 | 收入/支出筛选 | 点击"收入"或"支出"筛选按钮 | 仅显示对应类型的交易记录 |
| M3-04 | 搜索功能 | 在搜索框输入关键字 | 列表实时过滤匹配描述的记录 |
| M3-05 | 月度统计卡片 | 查看页面顶部统计 | 显示本月总收入、总支出、本月盈余、当前余额，均为数字（非 NaN） |

#### M3.2 创建交易

| 编号 | 用例 | 操作步骤 | 预期结果 |
|------|------|---------|---------|
| M3-06 | API 创建收入 | POST /api/transactions，type=income, amount=5000, account_id=有效ID, subject_id=收入科目ID | 返回 201，success=true，data 含新交易 ID |
| M3-07 | API 创建支出 | POST /api/transactions，type=expense, amount=2000 | 返回 201，success=true |
| M3-08 | ⭐ 收入后余额增加 | 创建收入交易前后查询账户余额 | 账户 balance 增加 amount 数额 |
| M3-09 | ⭐ 支出后余额减少 | 创建支出交易前后查询账户余额 | 账户 balance 减少 amount 数额 |
| M3-10 | 交易出现在列表中 | 创建交易后刷新出入金页面 | 新交易出现在列表顶部 |

#### M3.3 验证规则

| 编号 | 用例 | 操作步骤 | 预期结果 |
|------|------|---------|---------|
| M3-11 | 金额为空 | POST，amount 不传或为空 | 返回 400，"金额必须大于0" |
| M3-12 | 金额为 0 | POST，amount=0 | 返回 400，"金额必须大于0" |
| M3-13 | 金额为负数 | POST，amount=-500 | 返回 400，"金额必须大于0" |
| M3-14 | 无效交易类型 | POST，type="abc" | 返回 400，"交易类型无效" |
| M3-15 | 缺少 type | POST，不传 type | 返回 400，"类型不能为空" |
| M3-16 | 日期自动填充 | POST，不传 transaction_date | 交易创建成功，日期为当天 |

---

### M4 内部划款（12 项）

路径: `/internal-transactions`

#### M4.1 划款功能

| 编号 | 用例 | 操作步骤 | 预期结果 |
|------|------|---------|---------|
| M4-01 | 页面加载 | 导航到内部划款页面 | 显示划款记录列表或空状态 |
| M4-02 | API 同币种划款 | POST，type=transfer, amount=5000, account_id=1, target_account_id=2 | 返回 201，包含 out_transaction 和 in_transaction |
| M4-03 | ⭐ 转出账户余额减少 | 划款前后查询转出账户余额 | balance 减少 amount |
| M4-04 | ⭐ 转入账户余额增加 | 划款前后查询转入账户余额 | balance 增加 to_amount |
| M4-05 | 含手续费划款 | POST，amount=10000, fees=50 | 转出账户减少 10050，转入账户增加 10000 |
| M4-06 | 跨币种划款 | POST，amount=10000, to_amount=1400（CNY→USD） | 转出减 10000 CNY，转入加 1400 USD |

#### M4.2 验证规则

| 编号 | 用例 | 操作步骤 | 预期结果 |
|------|------|---------|---------|
| M4-07 | 转出账户为空 | POST，不传 account_id | 返回 400，"转出账户不能为空" |
| M4-08 | 转入账户为空 | POST，不传 target_account_id | 返回 400，"转入账户不能为空" |
| M4-09 | 转出=转入 | POST，account_id=1, target_account_id=1 | 返回 400，"转出和转入账户不能相同" |
| M4-10 | 金额为 0 | POST，amount=0 | 返回 400，"划款金额必须大于0" |
| M4-11 | 金额为负 | POST，amount=-100 | 返回 400，"划款金额必须大于0" |

#### M4.3 数据一致性

| 编号 | 用例 | 操作步骤 | 预期结果 |
|------|------|---------|---------|
| M4-12 | ⭐ 划款事务一致性 | 划款成功后查询 transactions 表 | 生成两条记录（转出+转入），金额一致，type=transfer |

---

### M5 仪表板与报表（10 项）

路径: `/`

#### M5.1 数据展示

| 编号 | 用例 | 操作步骤 | 预期结果 |
|------|------|---------|---------|
| M5-01 | 页面加载 | 导航到仪表板 | 页面正常渲染，无白屏、无 loading 卡死 |
| M5-02 | 无 NaN 显示 | 检查所有数字显示区域 | 无 NaN、undefined、null、Infinity 出现 |
| M5-03 | 财务概览卡片 | 查看顶部统计卡片 | 显示总资产、总负债、净资产、运营现金，均为格式化货币值 |
| M5-04 | 日报/月报切换 | 点击日报/月报切换按钮 | 数据和图表跟随切换，无报错 |

#### M5.2 数据准确性

| 编号 | 用例 | 操作步骤 | 预期结果 |
|------|------|---------|---------|
| M5-05 | ⭐ 账户摘要交叉验证 | 对比 GET /api/dashboard 的 accountSummary 与 GET /api/accounts 的实际余额汇总 | 按币种的总余额一致 |
| M5-06 | ⭐ 交易摘要交叉验证 | 对比 dashboard.transactionSummary 与 GET /api/transactions 的实际记录 | 本月收入/支出笔数和金额一致 |
| M5-07 | 收入科目分析 | 查看收入按科目分析 | 各科目金额之和 = 总收入 |
| M5-08 | 支出科目分析 | 查看支出按科目分析 | 各科目金额之和 = 总支出 |
| M5-09 | 部门支出分析 | 查看按部门支出分析 | 有部门的支出记录正确归类 |
| M5-10 | API 综合数据 | GET /api/dashboard?projectId=1 | 返回 success=true，data 包含 5 个维度数据 |

---

### M6 配置管理（16 项）

#### M6.1 币种管理

路径: `/configurations/account-categories`（或相关配置页面）

| 编号 | 用例 | 操作步骤 | 预期结果 |
|------|------|---------|---------|
| M6-01 | 查看币种列表 | GET /api/currency-types?projectId=1 | 返回 CNY/USD/EUR/JPY 等 |
| M6-02 | 创建币种 | POST，name=测试币, code=TST | 返回 201，创建成功 |
| M6-03 | 重复代码拒绝 | POST，再次使用 code=TST | 返回错误，拒绝重复 |
| M6-04 | 更新币种 | PUT /api/currency-types/:id，修改 name | 返回更新后数据 |
| M6-05 | 删除币种 | DELETE /api/currency-types/:id | 删除成功，列表中消失 |

#### M6.2 账户类型管理

| 编号 | 用例 | 操作步骤 | 预期结果 |
|------|------|---------|---------|
| M6-06 | 查看账户类型列表 | GET /api/account-types?projectId=1 | 返回已配置的账户类型列表 |
| M6-07 | 创建账户类型 | POST，name=测试类型 | 返回 201 |

#### M6.3 科目管理

| 编号 | 用例 | 操作步骤 | 预期结果 |
|------|------|---------|---------|
| M6-08 | 查看全部科目 | GET /api/subjects?projectId=1 | 返回收入和支出科目 |
| M6-09 | 按类型筛选科目 | GET /api/subjects?projectId=1&type=income | 仅返回 type=income 的科目 |
| M6-10 | 创建收入科目 | POST，name=新收入科目, type=income, code=new-income | 创建成功 |
| M6-11 | 创建支出科目 | POST，name=新支出科目, type=expense, code=new-expense | 创建成功 |
| M6-12 | UI 收入科目 Tab | 点击收入科目分类 | 仅显示 type=income 的科目 |
| M6-13 | UI 支出科目 Tab | 点击支出科目分类 | 仅显示 type=expense 的科目 |

#### M6.4 部门管理

| 编号 | 用例 | 操作步骤 | 预期结果 |
|------|------|---------|---------|
| M6-14 | 查看部门列表 | GET /api/departments?projectId=1 | 返回财务部/市场部/技术部/人事部等 |
| M6-15 | 创建部门 | POST，name=新部门, code=new-dept | 创建成功 |
| M6-16 | 重复部门代码 | POST，再次使用 code=new-dept | 拒绝重复代码 |

---

### M7 人员管理（8 项）

#### M7.1 用户管理

路径: `/personnel/users`

| 编号 | 用例 | 操作步骤 | 预期结果 |
|------|------|---------|---------|
| M7-01 | 用户列表加载 | 导航到用户管理页面 | 显示当前项目的用户列表 |
| M7-02 | 管理员存在 | 查看列表 | 包含 admin 用户，角色为 admin |
| M7-03 | ⭐ 不暴露密码 | GET /api/users?projectId=1 | 返回数据中无 password 字段 |
| M7-04 | 编辑用户 | PUT /api/users/:id，修改 full_name | 更新成功，列表反映新名称 |
| M7-05 | ⭐ 禁止修改密码 | PUT /api/users/:id，body 含 password 字段 | 密码不被修改（后端 unset） |

#### M7.2 部门页面

路径: `/configurations/departments` 或 `/personnel/departments`

| 编号 | 用例 | 操作步骤 | 预期结果 |
|------|------|---------|---------|
| M7-06 | 部门列表 UI | 导航到部门管理页面 | 显示财务部、市场部、技术部、人事部 |
| M7-07 | 添加部门按钮 | 查看页面 | 存在"添加部门"按钮 |
| M7-08 | UI 创建部门 | 点击添加，填写名称和代码，提交 | 新部门出现在列表中 |

---

### M8 活动日志（6 项）

路径: `/personnel/activity-logs`（或 API 直接验证）

| 编号 | 用例 | 操作步骤 | 预期结果 |
|------|------|---------|---------|
| M8-01 | 查询日志 | GET /api/activity-logs?projectId=1 | 返回日志列表 |
| M8-02 | ⭐ 创建账户自动记日志 | 创建账户后查询日志 | 出现 action=create, target_type=accounts 的记录 |
| M8-03 | ⭐ 创建交易自动记日志 | 创建交易后查询日志 | 出现 action=create, target_type=transactions 的记录 |
| M8-04 | ⭐ 内部划款自动记日志 | 执行划款后查询日志 | 出现 action=transfer 的记录 |
| M8-05 | ⭐ 删除账户自动记日志 | 删除账户后查询日志 | 出现 action=delete, target_type=accounts 的记录 |
| M8-06 | 日志分页 | GET /api/activity-logs?page=1&limit=10 | 返回分页数据，pagination 信息正确 |

---

### M9 数据安全（8 项）

| 编号 | 用例 | 操作步骤 | 预期结果 |
|------|------|---------|---------|
| M9-01 | ⭐ 项目数据隔离 | GET /api/accounts?projectId=99999 | 返回空数组，不泄露其他项目数据 |
| M9-02 | SQL 注入防护 | GET /api/accounts?projectId=1 OR 1=1 | 不返回额外数据，PDO 参数化查询生效 |
| M9-03 | XSS 存储 | 创建 description 含 `<script>alert(1)</script>` 的交易 | 前端渲染时不执行脚本（React 自动转义） |
| M9-04 | CORS 配置 | 从不同域发送 OPTIONS 预检请求 | 返回正确的 CORS 头 |
| M9-05 | PUT /users 不改密码 | PUT 请求 body 含 password 字段 | password 被后端 unset，密码不变 |
| M9-06 | 越权访问账户 | 用用户 A 的 token 操作用户 B 独有的账户 | 拒绝或返回 404 |
| M9-07 | 健康检查公开 | GET /api/health（不带 token） | 返回 200，status=healthy |
| M9-08 | 注册接口禁用 | POST /api/register | 返回 501 Not Implemented |

---

### M10 UI 交互与响应式（8 项）

| 编号 | 用例 | 操作步骤 | 预期结果 |
|------|------|---------|---------|
| M10-01 | 侧边栏导航完整 | 登录后检查侧边栏 | 包含仪表板、资产记录、出入金、内部划款、配置管理、人员管理等一级菜单 |
| M10-02 | 导航跳转正确 | 依次点击各菜单项 | URL 和页面内容正确切换 |
| M10-03 | 移动端适配 | 将视口设为 375×812 | 无水平溢出滚动，布局自适应 |
| M10-04 | 移动端侧边栏 | 移动端视口下操作 | 侧边栏可折叠/展开，不遮挡内容 |
| M10-05 | 表格空状态 | 无数据的页面 | 显示友好的空状态提示（如"暂无数据"），非空白 |
| M10-06 | 加载状态 | 页面数据加载中 | 显示 loading 动画或骨架屏，非卡死 |
| M10-07 | Toast 提示 | 执行创建/删除等操作 | 显示成功/失败的 Toast 提示 |
| M10-08 | 对话框关闭 | 打开创建表单，按 ESC 或点击遮罩 | 对话框正常关闭 |

---

## 三、关键业务流程端到端测试

### E2E-01: 完整记账流程

```
1. 登录系统
2. 进入配置管理 → 确认币种/科目/部门已存在
3. 进入账户管理 → 创建一个新账户（CNY 活期，初始余额 100,000）
4. 进入出入金 → 创建一笔收入 ¥50,000（选择新账户、收入科目）
5. 验证：账户余额变为 ¥150,000
6. 创建一笔支出 ¥20,000（选择新账户、支出科目）
7. 验证：账户余额变为 ¥130,000
8. 进入仪表板 → 验证总资产、收支摘要反映了新数据
9. 进入活动日志 → 验证记录了创建账户和交易的操作日志
10. 删除测试账户，验证删除成功
```

### E2E-02: 内部划款流程

```
1. 登录系统
2. 记录账户 A 和账户 B 的当前余额
3. 进入内部划款 → 执行从 A 到 B 的划款 ¥10,000
4. 验证：A 余额减少 ¥10,000，B 余额增加 ¥10,000
5. 进入出入金 → 验证出现两条 transfer 类型记录
6. 进入活动日志 → 验证记录了划款操作
```

### E2E-03: 配置变更影响验证

```
1. 登录系统
2. 添加新币种 GBP
3. 进入账户管理 → 创建一个 GBP 账户
4. 验证：账户创建成功，币种为 GBP
5. 进入出入金 → 查看币种 Tab，应出现 GBP
6. 删除测试数据
```

---

## 四、测试环境与工具

| 项目 | 说明 |
|------|------|
| 测试环境 | EC2 Headless Chromium (Playwright MCP) |
| 浏览器 | Chromium (Playwright 安装) |
| API 测试 | Node.js fetch / curl |
| 截图目录 | /home/ubuntu/OA-System/test-screenshots/ |
| 报告格式 | JSON + 控制台输出 |

---

## 五、测试通过标准

- **P0 用例**: 100% 通过
- **P1 用例**: 95% 以上通过，未通过项有明确计划
- **P2 用例**: 90% 以上通过
- **无 P0 级别 Bug**（数据错误、安全漏洞、核心流程不通）
- **无页面 NaN / undefined / 白屏**
